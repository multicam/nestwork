<script>
    const {stringify} = JSON
    export async function load({page, fetch, session, context}) {
      log(page)
      return {
        status: 200,
        error: new Error(`Can't even...`)
      };
    }
</script>
<pre>coucou</pre>
